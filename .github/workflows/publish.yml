name: Publish to Comfy registry
on:
  workflow_dispatch:
  pull_request:

jobs:
  publish-node:
    name: Publish Custom Node to registry
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4
        with:
          submodules: true
      - name: Debug step
        run: ls -la repositories/ultimate_sd_upscale
